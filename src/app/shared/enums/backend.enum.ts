export enum BackendControllerRoute {
  User = '/users',
  Trainer = '/trainer',
}

export enum TrainerParams {
  Update = '/update',
  Show = '/show',
}

export enum UserParams {
  Verify = '/verify',
  CreateGhostClient = '/create-ghost-client',
}
